# Core Brain configuration.
#
# Copy to `.env` and fill in what you need. This file lists ONLY variables the
# Brain itself reads. Product and Fly keep their own secrets in their own
# repositories; nothing here belongs to another service.
#
# The Brain is fully functional with this file absent: it starts, serves every
# API method, and answers from stored memory using its deterministic heuristic
# provider. Everything below is optional.
#
# Never commit a filled-in `.env`. It is gitignored.

# ---------------------------------------------------------------------------
# Provider selection
# ---------------------------------------------------------------------------
# Which LLM provider answers understanding/analysis requests.
#   heuristic - deterministic, offline, always available (default)
#   gemini    - calls Google Gemini, falls back to heuristic on any failure
# Equivalent to the transport's --provider flag, which wins over this value.
#BRAIN_LLM_PROVIDER=heuristic

# ---------------------------------------------------------------------------
# Observability
# ---------------------------------------------------------------------------
# Structured records go to stderr. Off by default: a library must not write to a
# host's stderr uninvited. Records carry identifiers, counts and reasons — never
# memory content, and never a credential.
#BRAIN_LOG_ENABLED=1
#BRAIN_LOG_LEVEL=info          # debug | info | warning | error
#BRAIN_LOG_FORMAT=json         # json (one object per line) | text

# ---------------------------------------------------------------------------
# Gemini provider
# ---------------------------------------------------------------------------
# Gemini is an inference provider, not the Brain's memory. The Brain still owns
# memory, context, identity, preferences and learning; the model only ever sees
# bounded context the Brain chose to send it.
#
# GEMINI_ENABLED is a separate, explicit opt-in so a stray key cannot silently
# start sending data to a third party.
#GEMINI_ENABLED=1
#GEMINI_API_KEY=
#GEMINI_MODEL=gemini-3.8-flash
#GEMINI_API_BASE=https://generativelanguage.googleapis.com
#GEMINI_TIMEOUT_SECONDS=30
#GEMINI_TEMPERATURE=0.2
#GEMINI_MAX_OUTPUT_TOKENS=2048
